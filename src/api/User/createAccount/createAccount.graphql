type Mutation {
  createAccount(
    userName: String!
    gender: String!
    age: Int!
    email: String!
    password: String!
    address1: String
    address2: String
  ): Boolean!
}

type Query {
  something: String!
}
